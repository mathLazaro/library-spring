spring:
  application:
    name: Demo Jpa
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update
      
